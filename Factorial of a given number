#include <stdio.h>
int factorial(int n) {
  if (n == 0) return 1;
  return n * factorial(n - 1);
}
int main() {
  int m;
  printf("Enter a positive integer: ");
  scanf("%d", &m);
  printf("Factorial of %d = %d\n", m, factorial(m));
  return 0;
}
